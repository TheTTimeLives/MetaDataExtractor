const assert = require('chai').assert
const seedDbs = require('../seed-dbs'); //have to set an export on the file before any of this will work

describe('Seeder', function (){
  it('It should seed correctly ... I dunno how to write unit tests', function (){
    //assert...
  })
})